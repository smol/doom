import { test } from 'ava';
import { ColorMap } from '../src/lumps/colormap';
import { Playpal } from '../src/lumps/playpal';
import { LumpData } from '../src/lumps/lump';

test('test playpal', t => {
  let data : LumpData = new LumpData();
  // let playpal : Playpal = new Playpal()
});

test('Test colormap lump', t => {


});
